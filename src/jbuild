(library
 ((name async_sendfile)
  (public_name async_sendfile)
  (libraries (
    async_unix
    core
    async_kernel.limiter_async
    ))
  (preprocess (pps (ppx_jane ppxlib.runner)))
  ))

(jbuild_version 1)
